using System;
using TextEditor;

class Program
{
    static void Main()
    {
        Menu.DisplayMenu();
        //while (true)
        //{
        //    ConsoleKeyInfo keyInfo = Console.ReadKey(true);

        //    if (keyInfo.Key == ConsoleKey.I && (keyInfo.Modifiers & ConsoleModifiers.Control) != 0)
        //    {
        //        Console.WriteLine("¡Has presionado Ctrl + I (Todo se ha guardado previamente)!");
        //        Menu.DisplayMenu();
        //    }
        //    else if (keyInfo.Key == ConsoleKey.Escape)
        //    {
        //        break;
        //    }
        //}

       
    }
}
